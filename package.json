{
    "name": "jtml",
    "version": "1.0.0",
    "description": "**jtml** is a lightweight JavaScript micro-library inspired by [HTMX](https://htmx.org), but built for the JSON age. It brings HTML templating back to the front of frontend development—where it belongs.",
    "main": "template.js",
    "directories": {
        "doc": "docs"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "npx esbuild src/main.js --bundle --minify --sourcemap --outfile=build.js --target=es6 --format=iife",
        "watch": "npx esbuild src/main.js --bundle --sourcemap --outfile=build.js --target=es6 --format=iife --watch",
        "terser": "terser build.js -o main-min.js -c -m"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "esbuild": "^0.25.5"
    },
    "dependencies": {
        "terser": "^5.44.1"
    }
}