{
    "name": "jtml",
    "version": "1.0.0",
    "description": "**jtml** is a lightweight JavaScript micro-library inspired by [HTMX](https://htmx.org), but built for the JSON age. It brings HTML templating back to the front of frontend development—where it belongs.",
    "main": "template.js",
    "directories": {
        "doc": "docs"
    },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "watch": "OUTPUT_PATH=dev npx rollup -c rollup.config.mjs --watch",
        "build": "NODE_ENV=production npx rollup -c rollup.config.mjs",
        "dev": "concurrently 'npm run watch' 'npx serve ./dev'"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@rollup/plugin-commonjs": "^29.0.0",
        "@rollup/plugin-node-resolve": "^16.0.3",
        "@rollup/plugin-replace": "^6.0.3",
        "@rollup/plugin-terser": "^0.4.4",
        "rollup": "^4.54.0"
    },
    "dependencies": {
        "concurrently": "^9.2.1",
        "serve": "^14.2.5",
        "terser": "^5.44.1"
    }
}